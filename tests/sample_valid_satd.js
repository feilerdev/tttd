/**
 * Simple Hello World function
 */

// TODO(tes.test): Using alert() for output is not ideal for production code.
// We should refactor to use proper logging framework that works in all JS environments
// but don't have time to implement that right now - will need to revisit this later.
function sayHello() {
  alert("Hello, World!");
}

// Call the function
sayHello();